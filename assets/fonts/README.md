`etro` now self-hosts Alliance No.1 locally for offline-safe typography:

- `Degarism Studio - Alliance No.1 Light.otf`

If you want to try a different font later, add the licensed files here, wire them into `assets/css/styles.css` with `@font-face`, and add them to `CORE_ASSETS` in `sw.js` so they are available offline.
